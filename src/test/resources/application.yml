spring:
  config.activate.on-profile: test
  output.ansi.enabled: ALWAYS
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    show_sql: true
    hibernate:
      ddl-auto: update
  batch:
    job:
      enabled: false 